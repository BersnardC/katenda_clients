# node_modules plano (estilo npm).
# Requerido para que el output "standalone" de Next.js sea autocontenido:
# con el virtual store de pnpm, los symlinks/junctions apuntan al workspace
# y el deploy del standalone a un VPS se rompe (MODULE_NOT_FOUND).
node-linker=hoisted
